# 🎨 Hex Color Generator (Mini Project)

A simple and fun JavaScript project that generates a **random hex color** such as `#3FA8F2`.  
Each click updates both the color preview box and the displayed hex code.

This project is part of my **JavaScript Refresher** series.

---

## 🚀 Features

-   Generates a **new random hex color** on button click
-   Updates the preview box background
-   Shows the exact hex code (e.g., `#A1C3FF`)
-   Clean, centered card layout
-   Beginner-friendly code structure
-   Automatically displays a random color on page load
-   Responsive and clean design

---

## 🧠 What I Learned

-   How to build hex colors using a string of characters (`0123456789ABCDEF`)
-   Using loops and `Math.random()` to create values
-   Updating DOM elements using `textContent` and `style.backgroundColor`
-   Event handling with `addEventListener()`
-   Centering content using Flexbox
-   Clean file separation (HTML, CSS, JS)

---

## 📂 File Structure

```
hex-color-generator-mini/
│
├─ index.html # Structure, button, and color card
├─ style.css # Layout and UI styling
└─ script.js # Random color logic and UI updates
```

## 💡 How to Use

1. Open **index.html** in your browser
2. Click **New Color**
3. A brand new random hex color appears
4. The preview box updates instantly

---
✨ A quick project perfect for practicing **DOM manipulation**, **event handling**, and **JavaScript randomization**.