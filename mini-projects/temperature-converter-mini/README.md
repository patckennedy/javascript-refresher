# 🌡️ Temperature Converter (Mini Project)

A simple beginner-friendly tool that converts temperatures between **Celsius (°C)** and **Fahrenheit (°F)** as the user types.  
This project is part of my **JavaScript Refresher** series.

---

## 🚀 Features

-   Convert **Celsius → Fahrenheit** instantly
-   Convert **Fahrenheit → Celsius** instantly
-   Automatic live update as you type
-   Basic input validation
-   Clean and minimal UI

---

## 🧠 What I Learned

-   Listening to input events with `addEventListener("input", ...)`
-   Converting text input into numbers using `parseFloat()`
-   Using simple formulas for temperature conversion
-   Updating UI dynamically with `.value`
-   Handling invalid user input (`isNaN`)

---

## 📂 File Structure

```
temperature-converter-mini/
│
├─ index.html # Input fields and UI structure
├─ style.css # Basic styling
└─ script.js # Conversion logic and event listeners
```

---

## 💡 How to Use

1. Open **index.html** in your browser.
2. Type into **either** input box (Celsius or Fahrenheit).
3. The other field updates automatically with the converted temperature.
